-- 迪奥呀汉化
-- 主群获取更多T^T1070748130
local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()

-- ====== 全新启动画面：透明背景 + 图片 + 彩色飘带 + 文字动画 ======
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "StartupScreen"
screenGui.IgnoreGuiInset = true
screenGui.Parent = game.CoreGui
screenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

-- 透明背景（整个Gui背景透明）
screenGui.ResetOnSpawn = false

-- 彩色飘带效果（可选）
local function CreateRibbons(parent)
    local colors = {
        Color3.fromRGB(255,0,0), Color3.fromRGB(255,165,0), Color3.fromRGB(255,255,0),
        Color3.fromRGB(0,255,0), Color3.fromRGB(0,127,255), Color3.fromRGB(75,0,130),
        Color3.fromRGB(148,0,211)
    }
    for i = 1, 6 do
        local ribbon = Instance.new("Frame")
        ribbon.Size = UDim2.new(0, 300, 0, 6)
        ribbon.Position = UDim2.new(-0.3, 0, math.random(20,80)/100, 0)
        ribbon.BackgroundColor3 = colors[i]
        ribbon.BorderSizePixel = 0
        ribbon.BackgroundTransparency = 0.4
        ribbon.Parent = parent

        local gradient = Instance.new("UIGradient")
        gradient.Color = ColorSequence.new({
            ColorSequenceKeypoint.new(0, colors[i]),
            ColorSequenceKeypoint.new(1, colors[(i%7)+1])
        })
        gradient.Rotation = 90
        gradient.Parent = ribbon

        local tweenInfo = TweenInfo.new(6, Enum.EasingStyle.Linear, Enum.EasingDirection.InOut, -1)
        local tween = game:GetService("TweenService"):Create(ribbon, tweenInfo, {Position = UDim2.new(1.3, 0, math.random(20,80)/100, 0)})
        tween:Play()
    end
end
CreateRibbons(screenGui)

-- 图片（初始居中，大小0.5）
local centerImage = Instance.new("ImageLabel")
centerImage.Size = UDim2.new(0.5, 0, 0.5, 0)
centerImage.Position = UDim2.new(0.5, -0.25, 0.5, -0.25) -- 因为Scale计算：0.5*0.5 = 0.25，所以偏移-0.25
centerImage.BackgroundTransparency = 1
centerImage.Image = "https://i.ibb.co/jZvv8ZQQ/52368421279078078101783766561675-1783766633276edit.jpg"
centerImage.ScaleType = Enum.ScaleType.Fit
centerImage.Parent = screenGui

-- 文字（初始位于图片下方，完全透明）
local textLabel = Instance.new("TextLabel")
textLabel.Size = UDim2.new(0.8, 0, 0.1, 0)
textLabel.Position = UDim2.new(0.5, -0.4, 0.5 + 0.25, 20)  -- 图片下方20像素
textLabel.BackgroundTransparency = 1
textLabel.Text = "欢迎使用MZ脚本中心"
textLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
textLabel.TextScaled = true
textLabel.Font = Enum.Font.SourceSansBold
textLabel.TextStrokeTransparency = 0
textLabel.TextStrokeColor3 = Color3.new(0, 0, 0)
textLabel.TextTransparency = 1  -- 初始完全透明
textLabel.Parent = screenGui

-- 给文字添加彩虹渐变效果
local textGradient = Instance.new("UIGradient")
textGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(255,0,0)),
    ColorSequenceKeypoint.new(0.16, Color3.fromRGB(255,165,0)),
    ColorSequenceKeypoint.new(0.33, Color3.fromRGB(255,255,0)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(0,255,0)),
    ColorSequenceKeypoint.new(0.66, Color3.fromRGB(0,127,255)),
    ColorSequenceKeypoint.new(0.83, Color3.fromRGB(75,0,130)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(148,0,211))
})
textGradient.Parent = textLabel

local TweenService = game:GetService("TweenService")

-- 第一步：等待3秒
task.wait(3)

-- 第二步：文字缓慢淡入（2秒）
local fadeInText = TweenService:Create(textLabel, TweenInfo.new(2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {TextTransparency = 0})
fadeInText:Play()
fadeInText.Completed:Wait()

-- 第三步：保持图片和文字显示3秒
task.wait(3)

-- 第四步：图片淡出并同时文字上移到中心
local fadeOutImage = TweenService:Create(centerImage, TweenInfo.new(2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {ImageTransparency = 1})
local moveTextUp = TweenService:Create(textLabel, TweenInfo.new(2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Position = UDim2.new(0.5, -0.4, 0.5, -0.05)}) -- 中心偏上0.05

fadeOutImage:Play()
moveTextUp:Play()
fadeOutImage.Completed:Wait()
centerImage:Destroy() -- 图片彻底移除

-- 第五步：文字停留3秒后淡出
task.wait(3)
local fadeOutText = TweenService:Create(textLabel, TweenInfo.new(1.5, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {TextTransparency = 1})
fadeOutText:Play()
fadeOutText.Completed:Wait()

-- 清除启动画面
screenGui:Destroy()

-- ====== 新增：全屏视频播放（原功能保留） ======
local function ShowVideoPopup()
    local videoGui = Instance.new("ScreenGui")
    videoGui.Name = "VideoPopup"
    videoGui.IgnoreGuiInset = true
    videoGui.Parent = game.CoreGui

    local mainFrame = Instance.new("Frame")
    mainFrame.Size = UDim2.new(1, 0, 1, 0)
    mainFrame.BackgroundColor3 = Color3.new(0, 0, 0)
    mainFrame.BackgroundTransparency = 0
    mainFrame.Parent = videoGui

    -- 尝试使用 WebView（适用于支持 WebView 的注入器）
    local webView = nil
    local webViewSupported = false

    if syn and syn.web_view then
        webView = syn.web_view
        webViewSupported = true
    elseif Krnl and Krnl.web_view then
        webView = Krnl.web_view
        webViewSupported = true
    elseif webview then
        webView = webview
        webViewSupported = true
    end

    if webViewSupported then
        local videoUrl = "https://cdn.jsdelivr.net/gh/nznb520/nznb@main/VID_20260724_215920.mp4"
        local html = [[
            <html>
            <head>
                <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
                <style>
                    body { margin: 0; overflow: hidden; background: black; }
                    video { width: 100vw; height: 100vh; object-fit: fill; display: block; }
                </style>
            </head>
            <body>
                <video id="vid" src="]] .. videoUrl .. [[" autoplay muted playsinline></video>
                <script>
                    var vid = document.getElementById('vid');
                    vid.play();
                    if (vid.requestFullscreen) {
                        vid.requestFullscreen().catch(()=>{});
                    }
                </script>
            </body>
            </html>
        ]]
        local webViewInstance = webView(html, 0, 0, 0, 0)
        webViewInstance.Parent = mainFrame
        if webViewInstance.Size then
            webViewInstance.Size = UDim2.new(1, 0, 1, 0)
        end
        if webViewInstance.Position then
            webViewInstance.Position = UDim2.new(0, 0, 0, 0)
        end
        videoGui.WebView = webViewInstance

        local closeBtn = Instance.new("TextButton")
        closeBtn.Size = UDim2.new(0, 60, 0, 40)
        closeBtn.Position = UDim2.new(1, -70, 0, 10)
        closeBtn.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
        closeBtn.BorderSizePixel = 0
        closeBtn.Text = "关闭"
        closeBtn.TextColor3 = Color3.new(1, 1, 1)
        closeBtn.Font = Enum.Font.SourceSansBold
        closeBtn.TextScaled = true
        closeBtn.Parent = mainFrame

        closeBtn.MouseButton1Click:Connect(function()
            if videoGui then
                if videoGui.WebView and videoGui.WebView.Destroy then
                    videoGui.WebView:Destroy()
                end
                videoGui:Destroy()
            end
        end)
    else
        -- 不支持 WebView 时尝试打开浏览器或复制链接
        if syn and syn.open_url then
            syn.open_url("https://cdn.jsdelivr.net/gh/nznb520/nznb@main/VID_20260724_215920.mp4")
        elseif open_url then
            open_url("https://cdn.jsdelivr.net/gh/nznb520/nznb@main/VID_20260724_215920.mp4")
        elseif shared and shared.open_url then
            shared.open_url("https://cdn.jsdelivr.net/gh/nznb520/nznb@main/VID_20260724_215920.mp4")
        else
            if setclipboard then
                setclipboard("https://cdn.jsdelivr.net/gh/nznb520/nznb@main/VID_20260724_215920.mp4")
                WindUI:Notify({
                    Title = "提示",
                    Content = "无法打开浏览器，视频链接已复制到剪贴板",
                    Duration = 5,
                    Icon = "clipboard"
                })
            else
                WindUI:Notify({
                    Title = "提示",
                    Content = "注入器不支持任何打开网页或复制链接的方式，视频链接: https://cdn.jsdelivr.net/gh/nznb520/nznb@main/VID_20260724_215920.mp4",
                    Duration = 10,
                    Icon = "alert"
                })
            end
        end
        videoGui:Destroy()
    end
end

task.wait(0.5)
ShowVideoPopup()

function gradient(text, startColor, endColor)
    local result = ""
    local length = #text
    for i = 1, length do
        local t = (i - 1) / math.max(length - 1, 1)
        local r = math.floor((startColor.R + (endColor.R - startColor.R) * t) * 255)
        local g = math.floor((startColor.G + (endColor.G - startColor.G) * t) * 255)
        local b = math.floor((startColor.B + (endColor.B - startColor.B) * t) * 255)
        local char = text:sub(i, i)
        result = result .. "<font color=\"rgb(" .. r ..", " .. g .. ", " .. b .. ")\">" .. char .. "</font>"
    end
    return result
end

local Confirmed = false
WindUI:Popup({
    Title = "欢迎使用MZ脚本中心",
    Icon = "https://i.ibb.co/1YtNJxGL/Image-1776560526876-770.jpg",
    IconThemed = true,
    Content = "欢迎使用MZ脚本中心 " .. gradient("WindUI", Color3.fromHex("#00FF87"), Color3.fromHex("#60EFFF")) .. " Library",
    Buttons = {
        {
            Title = "Cancel",
            Callback = function() end,
            Variant = "Secondary",
        },
        {
            Title = "Continue",
            Icon = "arrow-right",
            Callback = function() Confirmed = true end,
            Variant = "Primary",
        }
    }
})
repeat wait() until Confirmed

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local HttpService = game:GetService("HttpService")

local playerAvatarUrl = ""
local success, result = pcall(function()
    local response = game:HttpGet(string.format("https://thumbnails.roblox.com/v1/users/avatar?userIds=%d&size=150x150&format=Png&isCircular=true", LocalPlayer.UserId))
    if response then
        local data = HttpService:JSONDecode(response)
        if data and data.data and data.data[1] then
            playerAvatarUrl = data.data[1].imageUrl
        end
    end
end)

if not success or playerAvatarUrl == "" then
    playerAvatarUrl = "rbxassetid://129260712070622"
end

local Window = WindUI:CreateWindow({
    Title = "MZ脚本中心",
    Icon = playerAvatarUrl,
    IconThemed = true,
    Author = LocalPlayer.DisplayName .. " (@" .. LocalPlayer.Name .. ")",
    Folder = "MZ脚本中心",
    Size = UDim2.fromOffset(580, 460),
    Transparent = true,
    Background = "https://raw.githubusercontent.com/XxwanhexxX/UN/main/%E7%94%98%E5%9F%8E.png",
    BackgroundImageTransparency = 0.5,
    Theme = "Dark",
    User = {
        Enabled = true,
        Anonymous = false,
        Callback = function()
            print("点击了用户区域")
        end
    },
    SideBarWidth = 200,
    ScrollBarEnabled = true,
})

-- ====== 添加彩虹流动边框 ======
local function CreateRainbowBorder(parentFrame)
    local border = Instance.new("Frame")
    border.Name = "RainbowBorder"
    border.Size = UDim2.new(1, 8, 1, 8)  -- 边框向外扩展4像素
    border.Position = UDim2.new(0, -4, 0, -4)
    border.BackgroundColor3 = Color3.new(1,1,1)
    border.BackgroundTransparency = 0
    border.BorderSizePixel = 0
    border.ZIndex = parentFrame.ZIndex - 1
    border.Parent = parentFrame.Parent

    -- 彩虹渐变
    local rainbowGradient = Instance.new("UIGradient")
    rainbowGradient.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(255,0,0)),
        ColorSequenceKeypoint.new(0.16, Color3.fromRGB(255,165,0)),
        ColorSequenceKeypoint.new(0.33, Color3.fromRGB(255,255,0)),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(0,255,0)),
        ColorSequenceKeypoint.new(0.66, Color3.fromRGB(0,127,255)),
        ColorSequenceKeypoint.new(0.83, Color3.fromRGB(75,0,130)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(148,0,211))
    })
    rainbowGradient.Rotation = 0
    rainbowGradient.Parent = border

    -- 流动动画
    local offset = 0
    local connection = game:GetService("RunService").Heartbeat:Connect(function(dt)
        offset = offset + dt * 0.3  -- 流动速度
        if offset >= 1 then offset = offset - 1 end
        rainbowGradient.Offset = Vector2.new(offset, 0)
    end)

    -- 当主Frame被移除时断开连接
    parentFrame.AncestryChanged:Connect(function()
        if not parentFrame.Parent then
            connection:Disconnect()
            border:Destroy()
        end
    end)

    -- 确保边框始终在Window后面
    parentFrame:GetPropertyChangedSignal("ZIndex"):Connect(function()
        border.ZIndex = parentFrame.ZIndex - 1
    end)

    return border
end

-- 延迟确保Window的GUI已创建
task.spawn(function()
    wait(1)
    local windowFrame = nil
    -- 查找WindUI创建的主Frame，假设名称包含"Main"或使用Tag
    for _, child in ipairs(game.CoreGui:GetDescendants()) do
        if child:IsA("Frame") and child:FindFirstChild("Title") then
            local title = child:FindFirstChild("Title")
            if title:IsA("TextLabel") and title.Text == "MZ脚本中心" then
                windowFrame = child
                break
            end
        end
    end
    if windowFrame then
        CreateRainbowBorder(windowFrame)
    else
        warn("未找到主窗口框架")
    end
end)

Window:Tag({
    Title = "MZ脚本中心",
    Color = Color3.fromHex("00ffff")
})

Window:Tag({
    Title = "V2专属版",
    Color = Color3.fromHex("FFFF00")
})

Window:Tag({
    Title = "欢迎使用",
    Color = Color3.fromHex("00FF00")
})

Window:Tag({
    Title = "尊敬的〖竹影清风〗&〖墨龙‖凌霄〗团员＆团长＆副团长 您好",
    Color = Color3.fromHex("FFFFFF")
})

Window:EditOpenButton({
    Title = "打开〖MZ脚本中心〗",
    Icon = "monitor",
    CornerRadius = UDim.new(0,16),
    StrokeThickness = 2,
    Color = ColorSequence.new(
        Color3.fromHex("FF0F7B"),
        Color3.fromHex("F89B29")
    ),
    Draggable = true,
})

local Tabs = {}

Tabs.UniversalSection = Window:Section({
    Title = "通用",
    Opened = true,
})

Tabs.DevSection = Window:Section({
    Title = "开发选项",
    Opened = false,
})

Tabs.MostGamesSection = Window:Section({
    Title = "大部分服可用",
    Opened = false,
})

Tabs.ServerSection = Window:Section({
    Title = "服务器功能",
    Opened = true,
})

Tabs.UISection = Window:Section({
    Title = "UI库功能",
    Opened = true,
})

Tabs.InfoTab = Tabs.UniversalSection:Tab({ Title = "信息", Icon = "info" })
Tabs.DevToolsTab = Tabs.DevSection:Tab({ Title = "开发工具", Icon = "tool" })
Tabs.DevsTab = Tabs.DevSection:Tab({ Title = "开发人员", Icon = "users" })
Tabs.GeneralTab = Tabs.MostGamesSection:Tab({ Title = "通用", Icon = "settings" })
Tabs.ESPTab = Tabs.MostGamesSection:Tab({ Title = "ESP透视", Icon = "eye" })
Tabs.ServerSelectTab = Tabs.ServerSection:Tab({ Title = "选择服务器", Icon = "server" })
Tabs.UISettingsTab = Tabs.UISection:Tab({ Title = "UI设置", Icon = "palette" })

Window:SelectTab(1)

Tabs.InfoTab:Paragraph({
    Title = "欢迎使用MZ脚本中心",
    Desc = "祝您游玩愉快",
    Image = "rbxassetid://129260712070622",
    ImageSize = 42,
    Thumbnail = "https://i.ibb.co/5hfcs9hk/9b4d562261482726fa2847b6a5f44950fb770e9162156180ffb0461ef328abdf.webp",
    ThumbnailSize = 120,
})

Tabs.InfoTab:Divider()

Tabs.InfoTab:Paragraph({
    Title = "从今天起【竹影清风】军团与【墨龙 凌霄】军团手游上线之后将会开启【友军训练战】",
    Desc = "规则：（活动时间：出手游的第6周）\n注意：参赛人员必须是远古龙！并且拉复活的钱双方共出，并且打架期间不能使用技能只能使用普通的咬\n\n1.双方各出一个人与对方单挑活着则赢死者则输（切记友谊第1比赛第2！）\n2.所有普通成员打完之后便会有各个分队的队长继续训练（跟第1条一样）\n3.最后由各个团的副团长和团长相互配合攻打对方的团长和副团长（赢的军团将会获得6666龙币）",
    Image = "rbxassetid://129260712070622",
    ImageSize = 42,
    Thumbnail = "https://i.ibb.co/4RQp8S5X/Image-1776484102307-954.png",
    ThumbnailSize = 120,
})

Tabs.InfoTab:Divider()

Tabs.InfoTab:Paragraph({
    Title = "从今天起【竹影清风】军团与【墨龙 凌霄】军团手游上线之后将会开启【友军训练战】",
    Desc = "规则：（活动时间：出手游的第6周）\n注意：参赛人员必须是远古龙！并且拉复活的钱双方共出，并且打架期间不能使用技能只能使用普通的咬\n\n1.双方各出一个人与对方单挑活着则赢死者则输（切记友谊第1比赛第2！）\n2.所有普通成员打完之后便会有各个分队的队长继续训练（跟第1条一样）\n3.最后由各个团的副团长和团长相互配合攻打对方的团长和副团长（赢的军团将会获得6666龙币）",
    Image = "rbxassetid://129260712070622",
    ImageSize = 42,
    Thumbnail = "https://i.ibb.co/Hw908R6/Image-1776485613816-44.jpg",
    ThumbnailSize = 120,
})

Tabs.GeneralTab:Button({
    Title = "重新加入服务器",
    Desc = "点击重新加入当前游玩的服务器",
    Callback = function()
        game:GetService("TeleportService"):TeleportToPlaceInstance(game.PlaceId, game.JobId, game.Players.LocalPlayer)
    end
})

Tabs.GeneralTab:Divider()

Tabs.GeneralTab:Slider({
    Title = "玩家速度",
    Desc = "调整玩家移动速度",
    Step = 1,
    Value = {
        Min = 0,
        Max = 200,
        Default = 16,
    },
    Callback = function(value)
        local character = game.Players.LocalPlayer.Character
        if character then
            local humanoid = character:FindFirstChildOfClass("Humanoid")
            if humanoid then
                humanoid.WalkSpeed = value
            end
        end
    end
})

Tabs.GeneralTab:Slider({
    Title = "跳跃高度",
    Desc = "调整玩家跳跃高度",
    Step = 1,
    Value = {
        Min = 0,
        Max = 200,
        Default = 50,
    },
    Callback = function(value)
        local character = game.Players.LocalPlayer.Character
        if character then
            local humanoid = character:FindFirstChildOfClass("Humanoid")
            if humanoid then
                humanoid.JumpPower = value
            end
        end
    end
})

Tabs.GeneralTab:Slider({
    Title = "重力",
    Desc = "调整游戏重力",
    Step = 1,
    Value = {
        Min = 0,
        Max = 200,
        Default = 196.2,
    },
    Callback = function(value)
        workspace.Gravity = value
    end
})

Tabs.GeneralTab:Slider({
    Title = "当前血量",
    Desc = "调整玩家当前血量",
    Step = 1,
    Value = {
        Min = 0,
        Max = 1000,
        Default = 100,
    },
    Callback = function(value)
        local character = game.Players.LocalPlayer.Character
        if character then
            local humanoid = character:FindFirstChildOfClass("Humanoid")
            if humanoid then
                humanoid.Health = value
            end
        end
    end
})

Tabs.GeneralTab:Slider({
    Title = "血量上限",
    Desc = "调整玩家最大血量",
    Step = 1,
    Value = {
        Min = 0,
        Max = 1000,
        Default = 100,
    },
    Callback = function(value)
        local character = game.Players.LocalPlayer.Character
        if character then
            local humanoid = character:FindFirstChildOfClass("Humanoid")
            if humanoid then
                humanoid.MaxHealth = value
            end
        end
    end
})

Tabs.GeneralTab:Divider()

local isMapTransparent = false
local originalTransparencies = {}

Tabs.GeneralTab:Toggle({
    Title = "地图透视",
    Desc = "使地图变得半透明",
    Value = false,
    Callback = function(state)
        isMapTransparent = state
        if state then
            for _, part in pairs(workspace:GetDescendants()) do
                if part:IsA("BasePart") and part.Transparency < 0.5 then
                    originalTransparencies[part] = part.Transparency
                    part.Transparency = 0.7
                end
            end
        else
            for part, transparency in pairs(originalTransparencies) do
                if part and part.Parent then
                    part.Transparency = transparency
                end
            end
            originalTransparencies = {}
        end
    end
})

Tabs.GeneralTab:Divider()

Tabs.GeneralTab:Button({
    Title = "飞行",
    Desc = "启用飞行功能",
    Callback = function()
        loadstring(game:HttpGet('https://raw.githubusercontent.com/nznb520/MZ/refs/heads/main/%E9%A3%9E%E8%A1%8C'))()
    end
})

Tabs.GeneralTab:Button({
    Title = "飞车",
    Desc = "启用飞车功能",
    Callback = function()
        loadstring(game:HttpGet('https://raw.githubusercontent.com/nznb520/MZ/refs/heads/main/%E9%A3%9E%E8%BD%A6'))()
    end
})

Tabs.GeneralTab:Divider()

local playerNotifyEnabled = false
local PlayerJoinLeaveConnections = {}

Tabs.GeneralTab:Toggle({
    Title = "玩家进入时通知",
    Desc = "监听玩家进入和离开，显示通知",
    Value = false,
    Callback = function(state)
        playerNotifyEnabled = state
        
        if state then
            local function OnPlayerAdded(player)
                WindUI:Notify({
                    Title = "玩家进入",
                    Content = player.Name .. " 进入了服务器",
                    Duration = 5,
                    Icon = "user-plus"
                })
            end
            
            local function OnPlayerRemoving(player)
                WindUI:Notify({
                    Title = "玩家离开",
                    Content = player.Name .. " 离开了服务器",
                    Duration = 5,
                    Icon = "user-minus"
                })
            end
            
            local playerAddedConnection = Players.PlayerAdded:Connect(OnPlayerAdded)
            PlayerJoinLeaveConnections["PlayerAdded"] = playerAddedConnection
            
            local playerRemovingConnection = Players.PlayerRemoving:Connect(OnPlayerRemoving)
            PlayerJoinLeaveConnections["PlayerRemoving"] = playerRemovingConnection
            
            for _, player in pairs(Players:GetPlayers()) do
                if player ~= LocalPlayer then
                    WindUI:Notify({
                        Title = "玩家已存在",
                        Content = player.Name .. " 已在服务器中",
                        Duration = 3,
                        Icon = "user"
                    })
                end
            end
        else
            for name, connection in pairs(PlayerJoinLeaveConnections) do
                if connection then
                    connection:Disconnect()
                end
            end
            PlayerJoinLeaveConnections = {}
        end
    end
})

Tabs.DevToolsTab:Button({
    Title = "汉化版Dex",
    Desc = "点击加载汉化版Dex脚本",
    Callback = function()
        loadstring(game:HttpGet("https://api.luarmor.net/files/v3/loaders/4fe525637e43a1be8cb0cdf902d107c2.lua"))()
    end
})

Tabs.DevsTab:Paragraph({
    Title = "脚本作者:南烛",
    Desc = "脚本主要开发者",
    Image = "rbxassetid://129260712070622",
    ImageSize = 42,
    Thumbnail = "https://i.ibb.co/0Rd4GW6Q/Image-1757732064719.jpg",
    ThumbnailSize = 120,
})

Tabs.DevsTab:Paragraph({
    Title = "〖墨龙‖凌霄〗团长:逍遥",
    Desc = "墨龙凌霄团团长",
    Image = "rbxassetid://129260712070622",
    ImageSize = 42,
    Thumbnail = "https://i.ibb.co/DD5xCGKM/mmexport1776256139344.jpg",
    ThumbnailSize = 120,
})

Tabs.DevsTab:Paragraph({
    Title = "〖竹影清风〗团长:相信",
    Desc = "竹影清风团团长",
    Image = "rbxassetid://129260712070622",
    ImageSize = 42,
    Thumbnail = "https://i.ibb.co/99VFWgf6/Image-1776255807986-303.jpg",
    ThumbnailSize = 120,
})

Tabs.DevsTab:Paragraph({
    Title = "代理:深拥情话梦一场",
    Desc = "脚本代理",
    Image = "rbxassetid://129260712070622",
    ImageSize = 42,
    Thumbnail = "https://i.ibb.co/23v3Ghm1/Image-1776257620546-227.png",
    ThumbnailSize = 120,
})

Tabs.DevsTab:Paragraph({
    Title = "代理:以后",
    Desc = "脚本代理",
    Image = "rbxassetid://129260712070622",
    ImageSize = 42,
    Thumbnail = "https://i.ibb.co/NgTqH1gS/Image-1776255823756-537.jpg",
    ThumbnailSize = 120,
})

Tabs.DevsTab:Paragraph({
    Title = "社会",
    Desc = "纪律委员",
    Image = "rbxassetid://129260712070622",
    ImageSize = 42,
    Thumbnail = "https://i.ibb.co/XZvR4jLg/Image-1784903636065-359.jpg",
    ThumbnailSize = 120,
})  

Tabs.DevsTab:Paragraph({
    Title = "阿列克谢彼得罗夫",
    Desc = "墨龙凌霄团员",
    Image = "rbxassetid://129260712070622",
    ImageSize = 42,
    Thumbnail = "https://i.ibb.co/RGRRNDPW/Image-1784903539588-166.jpg",
    ThumbnailSize = 120,
})

Tabs.DevsTab:Paragraph({
    Title = "川溜",
    Desc = "墨龙凌霄3分队队长",
    Image = "rbxassetid://129260712070622",
    ImageSize = 42,
    Thumbnail = "https://i.ibb.co/FLzdKrkg/Image-1784903725966-803.jpg",
    ThumbnailSize = 120,
})

local ESPEnabled = false
local ESPHighlights = {}
local ESPConnections = {}

Tabs.ESPTab:Toggle({
    Title = "透视玩家",
    Desc = "透视所有玩家模型",
    Value = false,
    Callback = function(state)
        ESPEnabled = state
        
        if state then
            for _, connection in pairs(ESPConnections) do
                if connection then
                    connection:Disconnect()
                end
            end
            ESPConnections = {}
            
            local function AddHighlightToPlayer(player)
                if player ~= game.Players.LocalPlayer and player.Character then
                    local highlight = Instance.new("Highlight")
                    highlight.FillColor = Color3.fromRGB(255, 255, 255)
                    highlight.FillTransparency = 0.5
                    highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
                    highlight.OutlineTransparency = 0
                    highlight.Parent = player.Character
                    ESPHighlights[player] = highlight
                end
            end
            
            for _, player in pairs(game:GetService("Players"):GetPlayers()) do
                AddHighlightToPlayer(player)
            end
            
            local playerAddedConnection = game:GetService("Players").PlayerAdded:Connect(function(player)
                local characterAddedConnection = player.CharacterAdded:Connect(function(character)
                    if ESPEnabled then
                        wait(1)
                        local highlight = Instance.new("Highlight")
                        highlight.FillColor = Color3.fromRGB(255, 255, 255)
                        highlight.FillTransparency = 0.5
                        highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
                        highlight.OutlineTransparency = 0
                        highlight.Parent = character
                        ESPHighlights[player] = highlight
                    end
                end)
                
                ESPConnections[player] = characterAddedConnection
            end)
            
            ESPConnections["PlayerAdded"] = playerAddedConnection
            
            local playerRemovingConnection = game:GetService("Players").PlayerRemoving:Connect(function(player)
                if ESPHighlights[player] then
                    ESPHighlights[player]:Destroy()
                    ESPHighlights[player] = nil
                end
                
                if ESPConnections[player] then
                    ESPConnections[player]:Disconnect()
                    ESPConnections[player] = nil
                end
            end)
            
            ESPConnections["PlayerRemoving"] = playerRemovingConnection
        else
            for _, highlight in pairs(ESPHighlights) do
                if highlight then
                    highlight:Destroy()
                end
            end
            ESPHighlights = {}
            
            for _, connection in pairs(ESPConnections) do
                if connection then
                    connection:Disconnect()
                end
            end
            ESPConnections = {}
        end
    end
})

Tabs.ESPTab:Divider()

local RayDrawingEnabled = false
local RayLines = {}
local RayConnections = {}

local function GetScreenSize()
    return workspace.CurrentCamera.ViewportSize
end

local function CreateRayDrawing(player, character)
    if not character or not character.Parent then return end
    
    local screenSize = GetScreenSize()
    local startY = screenSize.Y / 2
    
    local line = Drawing.new("Line")
    line.Thickness = 2
    line.Color = Color3.fromRGB(255, 255, 255)
    line.From = Vector2.new(screenSize.X / 2, startY)
    
    RayLines[player] = line
    
    local function UpdateRay()
        if not RayDrawingEnabled or not character or not character.Parent then
            if RayLines[player] then
                RayLines[player]:Remove()
                RayLines[player] = nil
            end
            return
        end
        
        local torso = character:FindFirstChild("UpperTorso") or 
                     character:FindFirstChild("HumanoidRootPart") or
                     character:FindFirstChild("Torso")
        
        if torso then
            local camera = workspace.CurrentCamera
            local torsoPosition = torso.Position
            local torsoScreenPos, torsoVisible = camera:WorldToViewportPoint(torsoPosition)
            
            if torsoVisible then
                local screenPos = Vector2.new(torsoScreenPos.X, torsoScreenPos.Y)
                local line = RayLines[player]
                if line then
                    line.From = Vector2.new(screenSize.X / 2, screenSize.Y / 2)
                    line.To = screenPos
                    line.Visible = true
                end
            else
                if RayLines[player] then
                    RayLines[player].Visible = false
                end
            end
        end
    end
    
    local renderConnection = game:GetService("RunService").RenderStepped:Connect(UpdateRay)
    RayConnections[player] = renderConnection
end

local function CreateRaysForAllPlayers()
    for _, player in pairs(game:GetService("Players"):GetPlayers()) do
        if player ~= game.Players.LocalPlayer and player.Character then
            CreateRayDrawing(player, player.Character)
        end
    end
end

local function ClearAllRays()
    for player, line in pairs(RayLines) do
        if line then
            line:Remove()
        end
    end
    RayLines = {}
    
    for player, connection in pairs(RayConnections) do
        if connection then
            connection:Disconnect()
        end
    end
    RayConnections = {}
end

local RayToggle = Tabs.ESPTab:Toggle({
    Title = "开启绘制射线",
    Desc = "为所有玩家绘制指向身体的射线（固定从屏幕中心开始）",
    Value = false,
    Callback = function(state)
        RayDrawingEnabled = state
        
        if state then
            for _, connection in pairs(RayConnections) do
                if connection then
                    connection:Disconnect()
                end
            end
            RayConnections = {}
            
            CreateRaysForAllPlayers()
            
            local playerAddedConnection = game:GetService("Players").PlayerAdded:Connect(function(player)
                local characterAddedConnection = player.CharacterAdded:Connect(function(character)
                    wait(1)
                    if RayDrawingEnabled then
                        CreateRayDrawing(player, character)
                    end
                end)
                
                RayConnections[player] = characterAddedConnection
            end)
            
            RayConnections["PlayerAdded"] = playerAddedConnection
            
            local playerRemovingConnection = game:GetService("Players").PlayerRemoving:Connect(function(player)
                if RayLines[player] then
                    RayLines[player]:Remove()
                    RayLines[player] = nil
                end
                
                if RayConnections[player] then
                    RayConnections[player]:Disconnect()
                    RayConnections[player] = nil
                end
            end)
            
            RayConnections["PlayerRemoving"] = playerRemovingConnection
        else
            ClearAllRays()
        end
    end
})

Tabs.ServerSelectTab:Paragraph({
    Title = "温馨提示",
    Desc = "因为本脚本原名叫Atrumix 所以执行的脚本名字就叫这个本人懒得改 所以就这样吧 请见谅(๑•̀ㅁ•́ฅ)",
    Image = "https://i.ibb.co/PzTHmCvQ/Image-1776562211409-159.jpg",
    ImageSize = 42,
    ThumbnailSize = 120,
})

Tabs.ServerSelectTab:Divider()

local GameScripts = {
    [10253248401] = {"元素力量大亨", "https://raw.githubusercontent.com/nznb520/Atrumix/042524fac05940b1b1a9389a0bbb9572441b1bc3/%E5%85%83%E7%B4%A0%E5%8A%9B%E9%87%8F%E5%A4%A7%E4%BA%A8"},
    [79546208627805] = {"99夜", "https://raw.githubusercontent.com/nznb520/Atrumix/cca264ca9a72738d7d0f51b3e5bd31a764e9d0ec/99%E5%A4%9C"},
    [131716211654599] = {"鱼", "https://raw.githubusercontent.com/nznb520/Atrumix/881626be7e7cc72ac677c9cf375f3cb8c794eb55/%E9%B1%BC"},
    [13772394625] = {"刀刃球", "https://raw.githubusercontent.com/nznb520/Atrumix/b71ce91c007fd0cc5e3866c8c13ef8ee13ac17a9/%E5%88%80%E5%88%83%E7%90%83"},
    [124977557560410] = {"种植花园", "https://raw.githubusercontent.com/nznb520/Atrumix/045a07f1bbe87ad4d96f2ba8f2ab015db004bf20/%E7%A7%8D%E6%A4%8D%E8%8A%B1%E5%9B%AD"},
    [7541759836] = {"凹凸世界", "https://raw.githubusercontent.com/nznb520/Atrumix/main/%E5%87%B9%E5%87%B8%E4%B8%96%E7%95%8C.lua"},
    [3101667897] = {"极速传奇", "https://raw.githubusercontent.com/nznb520/MX-Script-Center/refs/heads/main/%E6%9E%81%E9%80%9F%E4%BC%A0%E5%A5%87"},
    [1537690962] = {"蜂群模拟器", "https://raw.githubusercontent.com/nznb520/MX-Script-Center/refs/heads/main/%E8%9C%82%E7%BE%A4%E6%A8%A1%E6%8B%9F%E5%99%A8"}
}

Tabs.ServerSelectTab:Button({
    Title = "自动检测并执行",
    Desc = "检测当前服务器并执行相应脚本",
    Callback = function()
        local placeId = game.PlaceId
        if GameScripts[placeId] then
            local gameName, scriptUrl = GameScripts[placeId][1], GameScripts[placeId][2]
            WindUI:Notify({
                Title = "检测到游戏",
                Content = "正在加载 " .. gameName .. " 脚本...",
                Duration = 3,
                Icon = "check"
            })
            loadstring(game:HttpGet(scriptUrl))()
        else
            WindUI:Notify({
                Title = "未找到脚本",
                Content = "当前游戏暂无可用脚本",
                Duration = 3,
                Icon = "alert"
            })
        end
    end
})

Tabs.ServerSelectTab:Divider()

for placeId, gameInfo in pairs(GameScripts) do
    local gameName, scriptUrl = gameInfo[1], gameInfo[2]
    
    Tabs.ServerSelectTab:Button({
        Title = gameName,
        Desc = "点击加载 " .. gameName .. " 脚本",
        Callback = function()
            WindUI:Notify({
                Title = "正在加载",
                Content = "加载 " .. gameName .. " 脚本...",
                Duration = 3,
                Icon = "download"
            })
            loadstring(game:HttpGet(scriptUrl))()
        end
    })
end

local HttpService = game:GetService("HttpService")
local folderPath = "WindUI"
makefolder(folderPath)

local function SaveFile(fileName, data)
    local filePath = folderPath .. "/" .. fileName .. ".json"
    local jsonData = HttpService:JSONEncode(data)
    writefile(filePath, jsonData)
end

local function LoadFile(fileName)
    local filePath = folderPath .. "/" .. fileName .. ".json"
    if isfile(filePath) then
        local jsonData = readfile(filePath)
        return HttpService:JSONDecode(jsonData)
    end
end

local function ListFiles()
    local files = {}
    for _, file in ipairs(listfiles(folderPath)) do
        local fileName = file:match("([^/]+)%.json$")
        if fileName then
            table.insert(files, fileName)
        end
    end
    return files
end

local themeValues = {}
for name, _ in pairs(WindUI:GetThemes()) do
    table.insert(themeValues, name)
end

local themeDropdown = Tabs.UISettingsTab:Dropdown({
    Title = "选择主题",
    Multi = false,
    AllowNone = false,
    Values = themeValues,
    Callback = function(theme)
        WindUI:SetTheme(theme)
    end
})
themeDropdown:Select(WindUI:GetCurrentTheme())

local ToggleTransparency = Tabs.UISettingsTab:Toggle({
    Title = "切换窗口透明度",
    Callback = function(e)
        Window:ToggleTransparency(e)
    end,
    Value = WindUI:GetTransparency()
})

Tabs.UISettingsTab:Divider()

Tabs.UISettingsTab:Section({ Title = "保存" })

local fileNameInput = ""
Tabs.UISettingsTab:Input({
    Title = "输入文件名",
    Placeholder = "输入文件名",
    Callback = function(text)
        fileNameInput = text
    end
})

Tabs.UISettingsTab:Button({
    Title = "保存文件",
    Callback = function()
        if fileNameInput ~= "" then
            SaveFile(fileNameInput, { Transparent = WindUI:GetTransparency(), Theme = WindUI:GetCurrentTheme() })
            WindUI:Notify({
                Title = "保存成功",
                Content = "配置已保存到 " .. fileNameInput .. ".json",
                Duration = 3,
                Icon = "check"
            })
        end
    end
})

Tabs.UISettingsTab:Divider()

Tabs.UISettingsTab:Section({ Title = "加载" })

local filesDropdown
local files = ListFiles()
filesDropdown = Tabs.UISettingsTab:Dropdown({
    Title = "选择文件",
    Multi = false,
    AllowNone = true,
    Values = files,
    Callback = function(selectedFile)
        fileNameInput = selectedFile
    end
})

Tabs.UISettingsTab:Button({
    Title = "加载文件",
    Callback = function()
        if fileNameInput ~= "" then
            local data = LoadFile(fileNameInput)
            if data then
                WindUI:Notify({
                    Title = "文件加载",
                    Content = "加载配置: " .. HttpService:JSONEncode(data),
                    Duration = 5,
                })
                if data.Transparent then
                    Window:ToggleTransparency(data.Transparent)
                    ToggleTransparency:SetValue(data.Transparent)
                end
                if data.Theme then
                    WindUI:SetTheme(data.Theme)
                    themeDropdown:Select(data.Theme)
                end
            end
        end
    end
})

Tabs.UISettingsTab:Button({
    Title = "覆盖保存",
    Callback = function()
        if fileNameInput ~= "" then
            SaveFile(fileNameInput, { Transparent = WindUI:GetTransparency(), Theme = WindUI:GetCurrentTheme() })
            WindUI:Notify({
                Title = "覆盖成功",
                Content = "配置已覆盖保存",
                Duration = 3,
                Icon = "check"
            })
        end
    end
})

Tabs.UISettingsTab:Button({
    Title = "刷新文件列表",
    Callback = function()
        filesDropdown:Refresh(ListFiles())
    end
})

Tabs.UISettingsTab:Divider()

Tabs.UISettingsTab:Section({ Title = "自定义主题" })

local currentThemeName = WindUI:GetCurrentTheme()
local themes = WindUI:GetThemes()
local ThemeAccent = themes[currentThemeName].Accent
local ThemeOutline = themes[currentThemeName].Outline
local ThemeText = themes[currentThemeName].Text
local ThemePlaceholderText = themes[currentThemeName].Placeholder

function updateTheme()
    WindUI:AddTheme({
        Name = currentThemeName,
        Accent = ThemeAccent,
        Outline = ThemeOutline,
        Text = ThemeText,
        Placeholder = ThemePlaceholderText
    })
    WindUI:SetTheme(currentThemeName)
end

local CreateInput = Tabs.UISettingsTab:Input({
    Title = "主题名称",
    Value = currentThemeName,
    Callback = function(name)
        currentThemeName = name
    end
})

Tabs.UISettingsTab:Colorpicker({
    Title = "背景颜色",
    Default = Color3.fromHex(ThemeAccent),
    Callback = function(color)
        ThemeAccent = color:ToHex()
    end
})

Tabs.UISettingsTab:Colorpicker({
    Title = "轮廓颜色",
    Default = Color3.fromHex(ThemeOutline),
    Callback = function(color)
        ThemeOutline = color:ToHex()
    end
})

Tabs.UISettingsTab:Colorpicker({
    Title = "文本颜色",
    Default = Color3.fromHex(ThemeText),
    Callback = function(color)
        ThemeText = color:ToHex()
    end
})

Tabs.UISettingsTab:Colorpicker({
    Title = "占位符文本颜色",
    Default = Color3.fromHex(ThemePlaceholderText),
    Callback = function(color)
        ThemePlaceholderText = color:ToHex()
    end
})

Tabs.UISettingsTab:Button({
    Title = "更新主题",
    Callback = function()
        updateTheme()
        WindUI:Notify({
            Title = "主题更新",
            Content = "主题已更新为 " .. currentThemeName,
            Duration = 3,
            Icon = "palette"
        })
    end
})

Window:OnClose(function()
    print("UI已关闭")
    
    for _, line in pairs(RayLines) do
        if line then
            line:Remove()
        end
    end
    RayLines = {}
    
    for _, connection in pairs(RayConnections) do
        if connection then
            connection:Disconnect()
        end
    end
    RayConnections = {}
    
    for _, connection in pairs(ESPConnections) do
        if connection then
            connection:Disconnect()
        end
    end
    ESPConnections = {}
    
    for _, connection in pairs(PlayerJoinLeaveConnections) do
        if connection then
            connection:Disconnect()
        end
    end
    PlayerJoinLeaveConnections = {}
end)
